<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <title>Arcadian Capital Inc. | Advisory, Restructuring & Risk</title>
  <meta name="description" content="Due diligence, restructuring planning, risk management, court officer roles, interim administration and liquidation services." />

  <style>
    :root{
      --bg: #0b1220;
      --card: rgba(255,255,255,.06);
      --text: rgba(255,255,255,.92);
      --muted: rgba(255,255,255,.72);
      --accent: #5eead4; /* teal */
      --accent2:#60a5fa; /* blue */
      --line: rgba(255,255,255,.12);
      --shadow: 0 12px 40px rgba(0,0,0,.35);
      --radius: 18px;
      --max: 1100px;
    }

    *{ box-sizing:border-box; }
    body{
      margin:0;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      color: var(--text);
      background:
        radial-gradient(1200px 700px at 15% 10%, rgba(96,165,250,.25), transparent 60%),
        radial-gradient(900px 600px at 80% 20%, rgba(94,234,212,.18), transparent 55%),
        linear-gradient(180deg, #070b14, #0b1220 55%, #050812);
      line-height: 1.55;
    }

    a{ color:inherit; text-decoration:none; }
    .wrap{ max-width: var(--max); margin:0 auto; padding: 0 20px; }

    /* Nav */
    header{
      position: sticky;
      top:0;
      z-index:10;
      backdrop-filter: blur(10px);
      background: rgba(11,18,32,.60);
      border-bottom: 1px solid var(--line);
    }
    .nav{
      display:flex;
      align-items:center;
      justify-content:space-between;
      padding: 14px 0;
      gap:16px;
    }
    .brand{
      display:flex;
      align-items:center;
      gap:12px;
      font-weight: 700;
      letter-spacing:.2px;
      white-space: nowrap;
    }
    .logo{
      width: 38px; height: 38px;
      border-radius: 12px;
      background: linear-gradient(135deg, var(--accent2), var(--accent));
      box-shadow: var(--shadow);
      flex: 0 0 auto;
    }

    .nav-right{
      display:flex;
      align-items:center;
      gap: 10px;
      flex-wrap: wrap;
      justify-content: flex-end;
    }

    nav ul{
      display:flex;
      list-style:none;
      padding:0;
      margin:0;
      gap: 8px;
      flex-wrap: wrap;
      justify-content: flex-end;
    }
    nav a{
      padding: 8px 10px;
      border-radius: 10px;
      color: var(--muted);
      display:inline-flex;
      align-items:center;
      gap: 8px;
    }
    nav a:hover{
      color: var(--text);
      background: rgba(255,255,255,.06);
    }

    .lang-btn{
      padding: 8px 10px;
      border-radius: 12px;
      border: 1px solid var(--line);
      background: rgba(255,255,255,.06);
      color: var(--text);
      font-weight: 700;
      cursor: pointer;
      line-height: 1;
    }
    .lang-btn:hover{ transform: translateY(-1px); }
    .lang-btn:active{ transform: translateY(0); }

    /* Hero */
    .hero{ padding: 70px 0 34px; }
    .hero-grid{
      display:grid;
      grid-template-columns: 1.2fr .8fr;
      gap: 26px;
      align-items: start;
    }
    h1{
      margin: 10px 0 10px;
      font-size: clamp(30px, 3.2vw, 48px);
      line-height: 1.1;
      letter-spacing: -0.6px;
    }
    .lead{
      color: var(--muted);
      font-size: 1.08rem;
      margin: 14px 0 22px;
      max-width: 62ch;
    }
    .cta-row{
      display:flex;
      gap: 12px;
      flex-wrap:wrap;
      margin: 18px 0 8px;
    }
    .btn{
      display:inline-flex;
      align-items:center;
      justify-content:center;
      gap: 10px;
      padding: 12px 16px;
      border-radius: 14px;
      border: 1px solid var(--line);
      background: rgba(255,255,255,.06);
      color: var(--text);
      font-weight: 600;
    }
    .btn.primary{
      background: linear-gradient(135deg, rgba(96,165,250,.9), rgba(94,234,212,.85));
      border: none;
      color: #041018;
    }
    .btn:hover{ transform: translateY(-1px); }
    .btn:active{ transform: translateY(0); }

    .hero-card{
      padding: 18px;
      background: var(--card);
      border: 1px solid var(--line);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
    }
    .hero-card h3{ margin: 0 0 10px; font-size: 1.05rem; }
    .pill{
      display:inline-flex;
      gap: 10px;
      align-items:center;
      padding: 10px 12px;
      border-radius: 14px;
      border: 1px solid var(--line);
      background: rgba(255,255,255,.04);
      margin: 8px 0;
      width: 100%;
    }
    .dot{
      width: 10px; height:10px; border-radius: 50%;
      background: var(--accent);
      box-shadow: 0 0 0 5px rgba(94,234,212,.12);
    }
    .pill b{ display:block; font-size:.95rem; }
    .pill span{ display:block; font-size:.9rem; color: var(--muted); }

    /* Sections */
    section{ padding: 48px 0; }
    .section-title{
      font-size: 1.55rem;
      margin: 0 0 8px;
      letter-spacing: -0.2px;
    }
    .section-sub{
      margin: 0 0 18px;
      color: var(--muted);
      max-width: 70ch;
    }
    .grid{
      display:grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 16px;
    }
    .card{
      padding: 18px;
      border-radius: var(--radius);
      background: var(--card);
      border: 1px solid var(--line);
      box-shadow: 0 10px 28px rgba(0,0,0,.25);
      min-height: 160px;
    }
    .card h4{ margin: 0 0 8px; font-size: 1.05rem; }
    .card p{ margin: 0; color: var(--muted); font-size: .98rem; }

    /* About */
    .about{
      display:grid;
      grid-template-columns: 1fr 1fr;
      gap: 16px;
      align-items: start;
    }
    .about .card{ min-height:auto; }

    /* Contact */
    .contact{
      display:grid;
      grid-template-columns: 1.1fr .9fr;
      gap: 16px;
      align-items: start;
    }
    form{ display:grid; gap: 10px; }
    input, textarea{
      width:100%;
      padding: 12px 12px;
      border-radius: 14px;
      border: 1px solid var(--line);
      background: rgba(255,255,255,.05);
      color: var(--text);
      outline:none;
    }
    textarea{ min-height: 120px; resize: vertical; }
    input::placeholder, textarea::placeholder{ color: rgba(255,255,255,.55); }
    .small{ font-size:.92rem; color: var(--muted); }

    footer{
      padding: 22px 0 34px;
      border-top: 1px solid var(--line);
      color: var(--muted);
    }
    .foot{
      display:flex;
      justify-content:space-between;
      align-items:center;
      gap: 10px;
      flex-wrap: wrap;
    }

    /* Responsive */
    @media (max-width: 900px){
      .hero-grid{ grid-template-columns: 1fr; }
      .grid{ grid-template-columns: 1fr 1fr; }
      .about{ grid-template-columns: 1fr; }
      .contact{ grid-template-columns: 1fr; }
    }
    @media (max-width: 560px){
      .grid{ grid-template-columns: 1fr; }
      .btn{ width: 100%; }
    }
  </style>
</head>

<body>
  <header>
    <div class="wrap nav">
      <div class="brand">
        <div class="logo" aria-hidden="true"></div>
        <div>Arcadian Capital Inc.</div>
      </div>

      <div class="nav-right">
        <nav aria-label="Primary navigation">
          <ul>
            <li><a href="#services" data-i18n="nav_services">Services</a></li>
            <li><a href="#approach" data-i18n="nav_approach">Approach</a></li>
            <li><a href="#about" data-i18n="nav_about">About</a></li>
            <li><a href="#contact" data-i18n="nav_contact">Contact</a></li>
          </ul>
        </nav>

        <!-- Language toggle -->
        <button class="lang-btn" id="langToggle" aria-label="Toggle language">FR</button>
      </div>
    </div>
  </header>

  <main>
    <!-- HERO -->
    <section class="hero">
      <div class="wrap hero-grid">
        <div>
          <p class="small" style="margin:0; letter-spacing:.08em; text-transform:uppercase;" data-i18n="hero_kicker">
            Advisory • Restructuring • Risk
          </p>

          <h1 data-i18n="hero_title">Clear guidance for complex financial situations.</h1>

          <p class="lead" data-i18n="hero_lead">
            We support investors, institutions, and organizations through due diligence, financial restructuring,
            risk management, and formal mandates related to insolvency and restructuring proceedings.
          </p>

          <div class="cta-row">
            <a class="btn primary" href="#contact" data-i18n="cta_consult">Request a consultation</a>
            <a class="btn" href="#services" data-i18n="cta_services">Explore services</a>
          </div>

          <p class="small" data-i18n="hero_values">Discreet • Rigorous • Execution-focused</p>
        </div>

        <aside class="hero-card">
          <h3 data-i18n="hero_card_title">Core mandates</h3>

          <div class="pill">
            <span class="dot" aria-hidden="true"></span>
            <div>
              <b data-i18n="pill1_title">Due diligence & advisory</b>
              <span data-i18n="pill1_sub">Decision support for investors and institutions</span>
            </div>
          </div>

          <div class="pill">
            <span class="dot" aria-hidden="true"></span>
            <div>
              <b data-i18n="pill2_title">Restructuring plans</b>
              <span data-i18n="pill2_sub">Design and implementation from strategy to execution</span>
            </div>
          </div>

          <div class="pill">
            <span class="dot" aria-hidden="true"></span>
            <div>
              <b data-i18n="pill3_title">Official roles & oversight</b>
              <span data-i18n="pill3_sub">Trustee/monitor/controller, interim administration, liquidation</span>
            </div>
          </div>
        </aside>
      </div>
    </section>

    <!-- SERVICES -->
    <section id="services">
      <div class="wrap">
        <h2 class="section-title" data-i18n="services_title">Services</h2>
        <p class="section-sub" data-i18n="services_sub">
          Practical financial expertise, structured processes, and reliable execution—tailored to stakeholders,
          legal contexts, and business realities.
        </p>

        <div class="grid">
          <div class="card">
            <h4 data-i18n="s1_title">Due Diligence & Business Advisory</h4>
            <p data-i18n="s1_text">
              Rigorous due diligence and strategic advisory services for investors, financial institutions,
              and government organizations—supporting informed decisions and value protection.
            </p>
          </div>

          <div class="card">
            <h4 data-i18n="s2_title">Financial Restructuring</h4>
            <p data-i18n="s2_text">
              Development and implementation of restructuring plans: stakeholder alignment, cash-flow stabilization,
              turnaround strategy, and execution support.
            </p>
          </div>

          <div class="card">
            <h4 data-i18n="s3_title">Financial & Operational Risk Management</h4>
            <p data-i18n="s3_text">
              Identification, assessment, and management of financial and operational risks, with frameworks
              that strengthen resilience and governance.
            </p>
          </div>

          <div class="card">
            <h4 data-i18n="s4_title">Court Officer Roles (Restructuring / Insolvency)</h4>
            <p data-i18n="s4_text">
              Appointment-ready support and official functions such as trustee/monitor/controller roles
              within restructuring proceedings and applicable legal frameworks.
            </p>
          </div>

          <div class="card">
            <h4 data-i18n="s5_title">Interim Administration & Liquidation</h4>
            <p data-i18n="s5_text">
              Acting as interim administrator or liquidator under corporate or consumer protection laws,
              or pursuant to a court order—ensuring transparent oversight and controlled execution.
            </p>
          </div>

          <div class="card">
            <h4 data-i18n="s6_title">Board & Public Company Advisory</h4>
            <p data-i18n="s6_text">
              Financial advisory services to boards of directors and investors of public companies,
              including complex decision support and governance-aligned analysis.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- APPROACH -->
    <section id="approach">
      <div class="wrap">
        <h2 class="section-title" data-i18n="approach_title">Our approach</h2>
        <p class="section-sub" data-i18n="approach_sub">
          We combine analytical rigor with real-world execution. Typical engagements follow a clear, disciplined path:
        </p>

        <div class="grid">
          <div class="card">
